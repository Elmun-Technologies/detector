[pytest]
testpaths = backend/tests
pythonpath = backend backend/tests
addopts = -q --strict-markers
markers =
    integration: requires real PostgreSQL, Redis and MinIO services (docker compose profile or CI service containers)
filterwarnings =
    ignore::DeprecationWarning
