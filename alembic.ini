[alembic]
script_location = backend/alembic
# Left empty on purpose: the URL comes from DATABASE_URL (app.config.settings)
# so the API container, the CI job and a local shell all migrate the database
# they are actually configured to use. Override with `-x` or
# `alembic -c ... -n ...` only for one-off maintenance.
sqlalchemy.url =
